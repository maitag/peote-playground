package audio.openAL;

// ------------------------------------------------
// ------------------- OpenAL ---------------------
// ------------------------------------------------

import haxe.io.Bytes;
import haxe.io.Float32Array;
import lime.media.openal.*;

class BufferOpenAL
{
	// TODO:

	/*
	public var backend(default, null):AudioOpenAL;
	public var buffer:ALBuffer;
	public var duration(default, null):Float;

	public function new(backend:AudioOpenAL, duration:Float) {
		this.backend = backend;
		this.duration = duration;
		buffer = AL.createBuffer();
	}

	public function setData(data:Float32Array) {
		AL.bufferData(buffer, AudioOpenAL.AL_FORMAT_MONO_FLOAT32,
			lime.utils.Float32Array.fromBytes(data.view.buffer),
			data.view.buffer.length, backend.sampleRate);
	}
	*/
}
